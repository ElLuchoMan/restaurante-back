# Formatea el código y aplica simplificaciones (-s), sobrescribe en sitio
gofmt -s -w .

# Ejecuta el linter con la configuración del repositorio
golangci-lint run

# Corre pruebas unitarias/rápidas en todos los paquetes
go test ./...

# Ejecuta cobertura completa, genera coverage.html y limpia archivos previos
powershell -ExecutionPolicy Bypass -File tools/cover.ps1 -Clean

# Genera/actualiza la documentación Swagger en docs/ sin levantar el servidor
swag init -g main.go -o docs

# Levanta la app con hot-reload y genera Swagger (bloquea la terminal)
bee run -downdoc=true -gendoc=true

# Limpia comentarios no-Swagger preservando build tags
# go run tools/comment_cleaner.go -root .
go run -tags tools tools/comment_cleaner.go -root .

# Instala hooks de git locales (pre-commit)
git config core.hooksPath .githooks
